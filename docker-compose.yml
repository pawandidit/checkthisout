version: '3.8'

services:
  dashboard:
    build: .
    container_name: financial-ml-dashboard
    ports:
      - "8050:8050"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - .:/app
    restart: unless-stopped
